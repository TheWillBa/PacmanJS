

class Points{
    constructor(x, y, val, type){
        this.x = x;
        this.y = y;
        this.sizeX = blockSize/10; // only for pellets, change later
        this.sizeY = blockSize/10; // only for pellets, change later
        this.val = val;
        this.type = type;
    }
}
